
export interface ServiceItem {
  id: string;
  title: string;
  description: string;
}

export interface ProductItem {
  id: string;
  title: string;
  category: string;
  image: string;
}

export interface ContactInfo {
  label: string;
  value: string;
  link: string;
}
